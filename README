This is a rails app created for tests of the new Gemfile.

All the tests are being runned with the "person.rb" model. Another rails app was created in other to received all the HTTP posts but it's not here on this repository. Still have not found no need to commit that here.

The gem's prototype is located in the /app/model/person.rb file in the 'Teletransporter' class.